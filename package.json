{
  "name": "pi-card",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "pi": { "extensions": ["./src/index.ts"] },
  "scripts": { "lint": "tsc --noEmit", "test": "vitest run",     "test:coverage": "vitest run --coverage",
    "eval:jev": "node --experimental-strip-types scripts/eval-jev.ts" },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.82.0",
    "@vitest/coverage-v8": "4.1.6",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  },
  "engines": { "node": ">=22" }
}
